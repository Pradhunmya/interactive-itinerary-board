import React from 'react';

const Footer: React.FC = () => (
    <footer className="app-footer">
        <span>© {new Date().getFullYear()} PlanPal Board — Crafted with care for travel lovers.</span>
    </footer>
);

export default Footer;